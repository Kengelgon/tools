### Github SSH keygen ###
# ssh-keygen -t ed25519 -C "your_email@example.com"
ssh-keygen -t ed25519 -C "kristianengel@gmail.com"

### Standard dependencies that may not be installed ###
sudo apt-get install 
	build-essential 
	zlib1g-dev 
	libffi-dev 
	libssl-dev 
	libbz2-dev 
	libreadline-dev 
	libsqlite3-dev 
	liblzma-dev

### GIT ###
git --rebase --help
git --rebase 

### New Env Setup ###

Visual Studio Code
Windows Subsystem for Linux

# Prefered install order
ZSH
oh-my-zsh
powerlevel10k

brew
curl
wget

pyenv
